# Copy this file to keystore.properties and fill in your real values.
# Do not commit keystore.properties or the keystore file itself.

# You can use an absolute path or a path relative to the repository root.
storeFile=path/to/release-keystore.jks
storePassword=replace-with-store-password
keyAlias=release
keyPassword=replace-with-key-password
