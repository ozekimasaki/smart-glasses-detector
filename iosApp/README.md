# iOS アプリ

SwiftUI + CoreBluetooth のクライアントです。検出ルールはリポジトリ直下の `:shared` Kotlin Multiplatform モジュールを `Shared.framework` としてリンクします。

詳細は [`docs/ios-build.md`](../docs/ios-build.md) を参照してください。

```bash
# macOS + Xcode
open iosApp/SmartGlassesDetector.xcodeproj
```
